# Docker Environment Configuration for Illegal Street

# PostgreSQL Database
POSTGRES_USER=illegal_street_user
POSTGRES_PASSWORD=SecurePassword123!
POSTGRES_DB=illegal_street_db

# Database URL for Backend
DATABASE_URL=postgresql://illegal_street_user:SecurePassword123!@postgres:5432/illegal_street_db

# Redis Configuration
REDIS_URL=redis://redis:6379

# JWT Configuration
# IMPORTANT: Generate secure secrets using: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long
JWT_REFRESH_SECRET=your_super_secret_jwt_refresh_key_min_32_characters_long
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# CORS Configuration
CORS_ORIGIN=http://localhost:8080

# API Configuration
API_VERSION=v1

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Email Configuration (Optional - configure if needed)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAIL_FROM=noreply@illegal-street.io

# Admin Configuration
ADMIN_EMAIL=admin@illegal-street.io

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
# IMPORTANT: Generate secure secret using: openssl rand -base64 32
SESSION_SECRET=your_session_secret_key_min_32_characters_long

# Encryption
# IMPORTANT: Must be exactly 32 characters. Generate using: openssl rand -base64 24
ENCRYPTION_KEY=your_encryption_key_32_characters_exactly

# Logging
LOG_LEVEL=info
LOG_DIR=./logs
